services:
  - type: web
    name: zkp-auth-framework
    runtime: python
    pythonVersion: "3.11.10"
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
